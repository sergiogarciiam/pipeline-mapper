stages:
  - build
  - test
  - deploy

variables:
  NODE_ENV: production
  VERSION: "1.2.3"

include: 
  - local: includes-base.yml
  - local: helper_multi2.yml

.prepare:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'

deploy2: 
  stage: deploy
  rules:
    - exists:
        - Dockerfile
        - helm/Chart.yaml

