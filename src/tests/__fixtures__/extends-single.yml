stages:
  - base
  - deploy

.base_job:
  stage: base
  script:
    - echo "Base script"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'

deploy:
  extends: .base_job
  stage: deploy
  script:
    - echo "Deploying extended job"